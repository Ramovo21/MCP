# Same required keys as staging.env.example, with a SEPARATE Supabase project,
# master key, worker token, Google client and domains. Never reuse staging secrets.
APP_ENV=production
WEB_DOMAIN=
GATEWAY_DOMAIN=
ACME_EMAIL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
DATABASE_URL=
MASTER_KEY=
WORKER_AUTH_TOKEN=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
